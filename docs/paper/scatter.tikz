\begin{tikzpicture}
\begin{axis}[
    title={Fluency Ratio vs. Benign Loss Degradation (Per Seed)},
    xlabel={Fluency Ratio (Poison / Control)},
    ylabel={Benign Loss Degradation (\%)},
    grid=major,
    width=10cm,
    height=8cm,
    legend pos=north west,
    % Threshold lines
    extra x ticks={1.5},
    extra x tick style={grid=major, grid style={thick,red,dashed}},
    extra y ticks={30},
    extra y tick style={grid=major, grid style={thick,red,dashed}},
]

\addplot[
    scatter,
    only marks,
    mark=*,
    mark size=2.5pt,
    % color=blue
] coordinates {
    % \textlangle DATA PENDING\textrangle
    % Format: (fluency_ratio, relative_degradation_percent)
};
\addlegendentry{SELECT Attack}

\end{axis}
\end{tikzpicture}
